import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({

    background: {
        width: '100%',
        height: '100%'
      },

    view: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }

});